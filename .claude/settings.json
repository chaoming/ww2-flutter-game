{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(echo '$TOOL_INPUT' | jq -r '.file_path // empty'); if [ -n \"$file_path\" ] && [[ \"$file_path\" == *.dart ]]; then dart format \"$file_path\" 2>/dev/null || true; fi"
          }
        ]
      }
    ]
  }
}
